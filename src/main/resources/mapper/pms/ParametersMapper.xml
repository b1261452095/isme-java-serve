<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.dhbin.isme.pms.mapper.ParametersMapper">
    <select id="pageDetail" resultType="cn.dhbin.isme.pms.domain.dto.ParametersPageDto">
        select u.id as id,
               u.sysName as sysName,
               u.sysCode as sysCode,
               u.sysValue as sysValue,
               u.enable as enable,
               u.createTime as createTime,
               u.updateTime as updateTIme,
        from `sys_parameter`
    </select>
</mapper>